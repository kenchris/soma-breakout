// === version.js === The release number shown in the bottom-right corner, so a player (or a tester on a
// phone) can see which version they are running. Bump it in every PR that changes the game (see CLAUDE.md;
// tests/run.mjs checks it was bumped against origin/main).
const APP_RELEASE = 19;
